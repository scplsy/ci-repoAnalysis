name: Create and publish bkrepo-aig image

on:
  push:
    tags:
      - "aig/v*"

jobs:
  build-and-push-image:
    uses: ./.github/workflows/deploy-golang-analysis-tool.yml
    with:
      tool_name: aig
